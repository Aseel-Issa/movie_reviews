

export default class Review {
    constructor(user_id, reviwer, rating, content){
        this.user_id = user_id
        this.reviwer = reviwer
        this.rating = rating
        this.content = content
    }
}